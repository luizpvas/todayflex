defmodule TodayflexWeb.Project.WidgetController do
  use TodayflexWeb, :controller

  @doc """
  GET /project/:project_id/widgets

  Lists widgets
  """
  def index(conn, params) do
  end

  @doc """
  POST /projects/:project_id/widgets

  Inserts a new widget for the given project.
  """
  def create(conn, params) do
  end

  @doc """
  PUT /projects/:project_id/widgets/:id

  Updates a widget
  """
  def update(conn, params) do
  end
end
